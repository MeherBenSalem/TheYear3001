package tn.nightbeam.robotica.procedures;

import tn.nightbeam.robotica.init.RoboticaModMenus;

import net.minecraft.world.entity.player.Player;
import net.minecraft.world.entity.Entity;

public class RemoveItemsFromCraftingGridProcedure {
	public static void execute(Entity entity) {
		if (entity == null)
			return;
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(0).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(1).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(2).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(3).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(4).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(5).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(6).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(7).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(8).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(9).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(10).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(11).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(12).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(13).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(14).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(15).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(16).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(17).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(18).remove(1);
			_player.containerMenu.broadcastChanges();
		}
		if (entity instanceof Player _player && _player.containerMenu instanceof RoboticaModMenus.MenuAccessor _menu) {
			_menu.getSlots().get(19).remove(1);
			_player.containerMenu.broadcastChanges();
		}
	}
}